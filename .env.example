# Next.js Application URL
# For local development, use http://localhost:3000
# For production, use your domain (e.g., https://yourdomain.com)
NEXTAUTH_URL=http://localhost:3000

# Note: Xero credentials are NOT stored in environment variables
# Users enter their Custom Connection credentials in the UI for each session
# This ensures no permanent storage of sensitive information
